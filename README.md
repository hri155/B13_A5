Solution:

    - 1️⃣ What is the difference between var, let, and const?
    
    let:allows to declare a variable that can be reassigned 

    const: values can't be changed once it assigned

    var: used to declare variables.(older way)
    

    - 2️⃣ What is the spread operator (...)?

       Spread operator (...) expands array into individual elements.

    - 3️⃣ What is the difference between map(), filter(), and forEach()?
   
    map(): Creates a new array by transforming each element of the original array.

    filter():Creates a new array with elements that pass a specified condition.

    forEach(): Executes a function for each element in an array without returning a new array.


    - 4️⃣ What is an arrow function?

    A shorter syntax for writing functions in JavaScript using => is called an arrow function


    - 5️⃣ What are template literals?

        Java create strings that are more powerful and easier to read than normal quotes.


    
    